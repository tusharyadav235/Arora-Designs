services:
  db:
    image: mysql:8.0
    container_name: arora-mysql
    restart: always
    environment:
      MYSQL_DATABASE: arora_db
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
